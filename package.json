{
  "name": "@f/evaluates",
  "description": "Check if syntax is supported.",
  "repository": "git://github.com/micro-js/evaluates.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
